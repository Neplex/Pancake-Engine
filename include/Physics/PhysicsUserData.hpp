//
// Created by kelle on 03/02/2017.
//

/*#ifndef PANCAKE_PHYSICSUSERDATA_HPP
#define PANCAKE_PHYSICSUSERDATA_HPP

namespace PancakeEngine {

     * @class PhysicsUserData
     * @brief A wrapper class for user data in Box2D for optimization.
     * @details Allow us to avoid a dynamic cast during Collision creation.
     * @example If the type is rigidbody, collider will be NULL and rigibody
will contain the rigidbody.
     * @deprecated Not very useful actually.
     */
// class PhysicsUserData {

/*public:

     * @enum Type
     * @brief Represent the type of the PhysicsUserData.

    enum Type {Rigidbody, Collider};

    PhysicsUserData(Type t, Rigidbody* rb, Collider* coll) : type(t),
rigidbody(rb), collider(coll) {}


    Rigidbody* rigidbody; ///< The rigibody data
    Collider* collider; ///< The collider data
    Type type; ///< The type of the data
};
public:


PhysicsUserData(Rigidbody* rb, Collider* coll){}

};

}

#endif //PANCAKE_PHYSICSUSERDATA_HPP*/
